node_modules/
.gitnexus/
.claude/
dist/
dist-renderer/
dist-electron/
.env
.env.local
*.env
*.env.local
service-account.json
push-to-firebase.js
AGENTS.md
*.tar.gz
*.exe
*.blockmap
cellhub-v2-FINAL-apr17-2012.json

# ──────────────────────────────────────────────────────────────
# R-CHORE-GITIGNORE-CRUFT: development workflow artifacts
# ──────────────────────────────────────────────────────────────
# Auditor/reparador handoff docs (created during dev sessions)
HANDOFF-AUDITOR.md
HANDOFF-*.md

# Intermediate diff files (generated during code review)
*.diff
*.patch

# Diff files saved as .txt (legacy naming)
*-diff.txt
diff*.txt

# Code snippets pasted for analysis
snippet*.txt
snippet*.md
scripts/.pt-cache.json

# ──────────────────────────────────────────────────────────────
# R-RELEASE-B1: code-signing material — NEVER commit certs/keys.
# (Signing secrets are supplied via env vars at build time; see
#  RELEASE_SIGNING.md.)
# ──────────────────────────────────────────────────────────────
*.pfx
*.p12
*.pem
*.key
*.cer
*.crt
certs/
